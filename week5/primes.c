#include <stdio.h>

int main (void) {
    int num;
    printf("This program prints prime numbers until a specified maximum");
    printf("Enter the maximum number to check");
    scanf("%d", &num);

    for (int i = 0; i <= num; i++) {
        
    }

    return 0;
}